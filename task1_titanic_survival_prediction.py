# -*- coding: utf-8 -*-
"""Task1_Titanic survival prediction.ipynb

Automatically generated by Colaboratory.

Original file is located at
    https://colab.research.google.com/drive/1CKJNQxZTgn2uakK_dVem6UKcajVvRDgI

Task_1: Titanic Survival Prediction

Importing Important Libraries
"""

import numpy as np
import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns

"""Importing Dataset

"""

df = pd.read_csv("tested.csv")
df.head(10)

df = pd.read_csv("train.csv")
df = pd.read_csv("test.csv")

df.shape

df.describe()

"""From Above cell it is clear there are some missing values in Age column

"""

df['Survived'].value_counts()

#let's visulize the count of survivals wrt pclass
sns.countplot(x=df['Survived'],hue=df['Pclass'])

df["Sex"]

#let's visualize the count of survivals wrt gender
sns.countplot(x=df['Sex'],hue=df['Survived'])

#Look at survival rate by sex
df.groupby('Sex')[['Survived']].mean()

df['Sex'].unique()
np.array(['male','female'], dtype=object)

from sklearn.preprocessing import LabelEncoder
labelencoder=LabelEncoder()
df['Sex']= labelencoder.fit_transform(df['Sex'])
df.head()

df['Sex'], df['Survived']

sns.countplot(x=df['Sex'],hue=df["Survived"])

df.isna().sum()

#After dropping non required column
df = df.drop(['Age'],axis=1)

df_final = df
df_final.head(10)

"""Model Training"""

x= df[['Pclass','Sex']]
y= df['Survived']

from sklearn.model_selection import train_test_split
x_train,x_test,y_train,y_test = train_test_split(x,y,test_size=0.2,random_state =0)

from sklearn.linear_model import LogisticRegression
log = LogisticRegression(random_state = 0)
log.fit(x_train, y_train)

"""Model Prediction

"""

pred = print(log.predict(x_test))

print(y_test)

import warnings
warnings.filterwarnings("ignore")
res= log.predict([[2,0]])
if(res==0):
   print("So Sorry! Not Survived")
else:
   print("Survived")